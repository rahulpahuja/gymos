node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Lockfiles — bun.lock is the source of truth
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment variables
.env*
!.env.example

# Secrets & credentials
# NOTE: firebase-applet-config.json holds only the client Firebase web config
# (API key, project/app IDs). These are not secrets - they ship in the browser
# bundle and are gated by Firestore rules + Auth settings - so it IS committed.
.firebaserc
*.pem
*.key
*.p12
*.keystore
service-account*.json
serviceAccount*.json
*-service-account.json
google-services.json
GoogleService-Info.plist
secrets.json
